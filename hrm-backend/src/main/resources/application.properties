spring.application.name=HRM
server.port=8080
server.address=${SERVER_ADDRESS:0.0.0.0}


spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=${DB_URL:jdbc:mysql://127.0.0.1:3306/HRM?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Ijse@123}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

jwt.secret=${JWT_SECRET:6YS6/n7egeA1Ezi5PpPWKTE7HQ9/4DKbfAKXjiJ1U6M=}
jwt.expiration=${JWT_EXPIRATION:86400000}
spring.main.allow-circular-references=true

# Enterprise security / governance defaults
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174,http://192.168.8.116:5173,http://192.168.8.116:5174,http://localhost:3000,http://167.172.95.86,http://pirisahr.com,https://pirisahr.com,http://www.pirisahr.com,https://www.pirisahr.com}
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.servlet.session.tracking-modes=cookie
server.use-forward-headers=true


# SMTP server configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SMTP_USERNAME:knowebtest@gmail.com}
spring.mail.password=${SMTP_PASSWORD:riem jmmz cyzh fqml}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

stripe.api.key=${STRIPE_API_KEY:}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:}
app.domain=${APP_DOMAIN:http://localhost:8080}

#app.domain=http://localhost:8080

# allow each file up to 50 MB
spring.servlet.multipart.max-file-size=50MB
# allow the entire request up to 50 MB (sum of all parts)
spring.servlet.multipart.max-request-size=50MB


# server.servlet.session.tracking-modes=cookie